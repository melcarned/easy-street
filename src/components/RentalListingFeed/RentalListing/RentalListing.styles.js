const styles = (theme) => ({
  post: {
    marginBottom: '1em',
  },
  neighborhoodTag: {
    display: 'inline-block',
    backgroundColor: '#0380C5',
    transformation: 'uppercase',
    padding: '3px 6px',
    borderRadius: '3px',
    color: '#fff'
  }
});

export default styles;