const styles = (theme) => ({
  rentalListingImageUploader: {
    marginTop: theme.spacing.unit * 1.5,
    marginBottom: theme.spacing.unit * 1.5,
  },
  photoPickerInput: {
    display: 'none',
  },
  photoPickerButtonText: {
    marginLeft: '10px'
  }
});

export default styles;