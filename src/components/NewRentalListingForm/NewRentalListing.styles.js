const styles = (theme) => ({
  newRentalListingDetails: {
    margin: theme.spacing.unit,
  },
  rentalListingFormField: {
    marginTop: theme.spacing.unit * 1.5,
    marginBottom: theme.spacing.unit * 1.5,
  }
});

export default styles;
